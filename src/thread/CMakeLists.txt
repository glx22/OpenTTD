add_files(thread.h)

if (NOT OPTION_USE_THREADS)
    add_files(thread_none.cpp)
elseif (WIN32)
    add_files(thread_win32.cpp)
elseif (OPTION_OS2)
    add_files(thread_os2.cpp)
else ()
    add_files(thread_pthread.cpp)
endif ()
